To run the system you have to run the tcp server separately and the main python system separate so for doing this run these two commands in separate terminals
1) python main.py
    on successful run it will show the file path and the number of the loaded instruments
2) python tcp_test_server.py
    on successful run it show the following message "🚀 TCP Server started on 127.0.0.1:9999"